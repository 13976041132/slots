syntax = "proto3";

package GPBClass.Message;

message PB_UserBind_Req {
    string openid = 1; //fb用户的标识
    string token = 2; //fb验证token
    int32 platform = 3; //平台标识
}

message PB_UserBind_Res {
    int32 uid = 1; //绑定后的用户ID
    string nickname = 2; //绑定后的昵称
    string avatar = 3; //绑定后的头像
}